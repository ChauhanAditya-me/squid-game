# Scripts Folder

Automation scripts for building and running the project:

- `quick-start.ps1`: Starts both backend and frontend servers (Windows)
- `build-backend.ps1`: Compiles and runs the backend
- `start-frontend.ps1`: Starts the frontend server

See `docs/SETUP.md` for usage instructions.
